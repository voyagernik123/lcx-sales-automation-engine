# Cloudflare Pages — monorepo settings
# Dashboard: Workers & Pages → lcx-sales-automation-engine → Settings → Builds

# Build command
#   npm install && npm run build
#
# Build output directory
#   apps/web/dist
#
# Root directory
#   /   (repository root)
#
# Environment variables
#   NODE_VERSION=20
