# /marketing before, RE-TAKEN with its fixtures

The eight-desk before in the parent directory was captured before the marketing fixtures existed, so its
`/marketing` row (2 figures) is an aborted-API reading. This capture — same tree, same frozen clock, fixtures
answering the summary, queue and perimeter — is the honest before for that desk: **8 figures** in the first
viewport, 0 page errors. The after uses the same fixtures.
