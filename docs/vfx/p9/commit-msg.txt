release(vfx): P9 THE PRODUCTION GATE — final sweep, the gallery beside the P0 baseline, the scorecard closed, the walkthrough; desktop v0.5.0 follows

THE PRODUCTION (VFX_PRODUCTION_PLAN.md) P9, on P8 ({{P8_SHA}}). The plan's line: "the finished platform, every route, both themes, in
GALLERY.md next to the P0 baseline; the desktop at the same build; a two-minute walkthrough of what to open."

WHAT IS HERE
  · docs/vfx/GALLERY.md regenerated by the final sweep (production-p9) — every route, both themes, GL on beside GL off — with the P0
    baseline's figures kept as the reference row.
  · docs/vfx/SCORECARD.md closed: every promise met / short / refused by design, each with its number and where it is read from.
  · docs/vfx/WALKTHROUGH.md: two minutes, what to open, what to look at, which record backs it.
  · The desktop release (v0.5.0) is its own commit, after this one is live — the app bundles this web build.

MEASURED (the final sweep)
  {{SWEEP}}

gate: {{GATE}}
