release(desktop): v0.5.0 — THE PRODUCTION P9: the desk at the same build as the web

THE PRODUCTION (VFX_PRODUCTION_PLAN.md) P9, the desktop half. The app bundles the P9 web build ({{WEB_SHA}}); the six version homes read
0.5.0 (apps/desktop/package.json, apps/web/package.json, tauri.conf.json, Cargo.toml, Cargo.lock's lcx-terminal block, Launch.tsx
LCXOS_VERSION), and the download page says what the file is.

THE DMG SIZE GUARD: the DMG is {{DMG_BYTES}} bytes ({{DMG_MB}} MB); LCXOS_DMG_MB moved {{OLD_MB}} → {{DMG_MB}} and the history line records the bytes;
the desktop was REBUILT after the move so the page inside the app agrees with the file it describes (the P4 lesson).

THE CHANNEL: latest.json on voyagernik123/lcx-terminal-releases carries version 0.5.0 and the minisign signature of the shipped
.app.tar.gz; CDN-verified after publish ({{CDN}}). The updater key has no passphrase; the app verifies against the committed pubkey.

HOST NOTE: built with /Library/Developer/CommandLineTools first on PATH — Xcode 26.6 (updated 2026-09-04 15:41) demands its licence
through /usr/bin/{git,python3,cc}; the CLT toolchain (Apple clang 21, cargo 1.97) does not, and the bundler calls xcrun only to notarize,
which this release does not.

gate: {{GATE}}
