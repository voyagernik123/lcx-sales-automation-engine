{
  "//": "esbuild bundles TypeScript by STRIPPING types; it does not verify them, so `build.mjs` going green says nothing about whether this file is sound. The repo's `type-check:3d` script points at docs/3d/p1 only, so e0/e1/e2/e5/e6/e8 are checked by nothing at all — E7 added a config of its own for exactly this reason and this is the same one. Run it: npx tsc -p docs/3d/e4/tsconfig.json",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2022", "DOM"],
    "strict": true,
    "noEmit": true,
    "skipLibCheck": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noUncheckedIndexedAccess": true,
    "isolatedModules": true,
    "esModuleInterop": true,
    "paths": { "@lcx/gl": ["../../../packages/gl/src/index.ts"] }
  },
  "include": ["entry.ts"]
}
