{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "LCXOS — the shell capabilities the operator desk needs.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-show",
    "core:window:allow-hide",
    "core:window:allow-set-focus",
    "core:window:allow-start-dragging",
    "core:event:default",
    "os:default",
    "process:default",
    "updater:default",
    "global-shortcut:default"
  ]
}
